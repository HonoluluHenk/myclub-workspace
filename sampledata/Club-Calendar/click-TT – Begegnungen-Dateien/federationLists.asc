var now = new Date().getTime();
var host = document.URL;
var fedBannerList100 = new Array();
function pushLeagueBannerList(arrayName, region, contestType, group, tag) {	
 if(typeof arrayName[region] != "undefined") { 
	if(typeof arrayName[region][contestType] != "undefined") { 
		if(typeof arrayName[region][contestType][group] != "undefined") { 
			arrayName[region][contestType][group].push(tag); 
		} else { 
			arrayName[region][contestType][group] = new Array(); 
			arrayName[region][contestType][group].push(tag); 
		} 
	} else { 
		arrayName[region][contestType] = new Array(); 
		arrayName[region][contestType][group] = new Array(); 
		arrayName[region][contestType][group].push(tag); 
	} 
 } else { 
	arrayName[region] = new Array(); 
	arrayName[region][contestType] = new Array(); 
	arrayName[region][contestType][group] = new Array(); 
	arrayName[region][contestType][group].push(tag); 
 } 
} 

function pushContestTypeBannerList(arrayName, region, contestType, tag) {	
 if(typeof arrayName[region] != "undefined") { 
	if(typeof arrayName[region][contestType] != "undefined") { 
	   arrayName[region][contestType].push(tag); 
	} else { 
		arrayName[region][contestType] = new Array(); 
		arrayName[region][contestType].push(tag); 
	} 
 } else { 
	arrayName[region] = new Array(); 
	arrayName[region][contestType] = new Array(); 
	arrayName[region][contestType].push(tag); 
 } 
} 

function pushChmpBannerList(arrayName, region, youngOld, league, group, tag) {	
 if(typeof arrayName[region] != "undefined") { 
 	if(typeof arrayName[region][youngOld] != "undefined") { 
		if(typeof arrayName[region][youngOld][league] != "undefined") { 
			if(typeof arrayName[region][youngOld][league][group] != "undefined") { 
				arrayName[region][youngOld][league][group].push(tag); 
			} else { 
				arrayName[region][youngOld][league][group] = new Array(); 
				arrayName[region][youngOld][league][group].push(tag); 
			} 
		} else { 
			arrayName[region][youngOld][league] = new Array(); 
			arrayName[region][youngOld][league][group] = new Array(); 
			arrayName[region][youngOld][league][group].push(tag); 
		} 
	} else { 
		arrayName[region][youngOld] = new Array() 
		arrayName[region][youngOld][league] = new Array(); 
		arrayName[region][youngOld][league][group] = new Array(); 
		arrayName[region][youngOld][league][group].push(tag); 
	} 
 } else { 
	arrayName[region] = new Array(); 
	arrayName[region][youngOld] = new Array(); 
	arrayName[region][youngOld][league] = new Array(); 
	arrayName[region][youngOld][league][group] = new Array(); 
	arrayName[region][youngOld][league][group].push(tag); 
 } 
} 

function Popup(url, args, linkText) {
    this.url = url;
    this.args = args;
    this.linkText = linkText;
}
if (1550049600000 <= now && now <= 1551682800000 && (host.search('localhost') >= 0 || host.search('banner.click-tt.de') >= 0 || host.search('click-tt.ch') >= 0)) {
  fedBannerList100[fedBannerList100.length] = '<a href="/cgi-bin/WebObjects/nuVerbandTTCH.woa/wa/click?redirect=https%3A%2F%2Fwww.sttelite2019.ch%2F" target="_blank"><img src="/banner/images/STT_Elite_2019_500x80.png" width="500" height="80" alt="SM Elite 2019"/></a>';
}
